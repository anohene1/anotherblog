/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com'], //make it 'your-domain.com'
    },
}

module.exports = nextConfig
